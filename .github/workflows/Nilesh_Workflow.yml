on:
  issues:
    types: [opened]

jobs:
  build:
    name: Greeting From Nilesh Chaglani
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run a multi-line script
      run: |
        echo Add other actions to build,
        echo test, and deploy your project.
